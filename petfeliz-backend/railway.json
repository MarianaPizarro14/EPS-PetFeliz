{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan route:clear && php artisan migrate --force && frankenphp run --config Caddyfile"
  }
}
